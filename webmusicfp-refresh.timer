# Refresh the episode list daily, and shortly after boot if a run was missed.
[Unit]
Description=Daily refresh of the Music For Programming episode list

[Timer]
OnCalendar=daily
RandomizedDelaySec=1h
Persistent=true

[Install]
WantedBy=timers.target
